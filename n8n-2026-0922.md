# ⚙️ n8n 自動化課程環境建置筆記

記錄了 2026/09/22 上課的 Node.js、Docker Desktop、VS Code、n8n 安裝流程
，以及 Antigravity CLI 的安裝與啟動方式。

---

## 📅 2026/09/22 ─ 環境安裝

### A. 安裝 Node.js

1. **安裝 Node.js**
2. 安裝後檢查版本：
   ```powershell
   node --version
   npm --version
   ```
3. **老師推薦使用 UV**

### B. 安裝 Docker Desktop

1. **安裝 Docker Desktop**
2. **安裝時選擇 all-users**
3. **安裝後啟動時選 skip**（跳過登入）
4. **驗證安裝**：
   ```powershell
   docker run hello-world
   ```

### C. 安裝 VS Code

* 安裝 `VSCodeSetup`

### D. 在 Docker 中部署與啟動 n8n (透過 VS Code or Zed)

1. 啟動 Docker
   > ⚠️ 若未啟動 Docker，部署 n8n 會出現錯誤訊息：
   > `failed to set up container networking`，無法完成部署。
2. 下載 `course.zip`：[Google Drive](https://drive.google.com/file/d/1BWJgF8OgN-LIOmIBODwbb9B2v7thDSxs/view)
3. 開啟 VS Code 輸入指令進行安裝（以 `code .` 可直接到正確路徑）：
   ```powershell
   docker-compose up -d
   ```
4. 若 port 衝突，變更 yml 與 env 的 port 即可 

### E. Other：安裝 Antigravity CLI

1. 在 PowerShell 輸入以下指令：
   ```powershell
   irm https://antigravity.google/cli/install.ps1 | iex
   ```
2. 以 `agy` 啟動
